# 液相色谱仪控制系统 - 详细业务逻辑 - 后端

## 后端分层架构设计

### 整体架构
后端采用五层架构设计，从上到下分别为：接口层、业务逻辑层、服务层、数据访问层、硬件抽象层。每一层都有明确的职责边界，通过接口进行通信。

### 1. 接口层（API Layer）
**职责说明**：
- 接收前端HTTP请求和WebSocket连接
- 进行请求参数验证和格式化
- 调用业务逻辑层处理请求
- 返回统一格式的响应数据
- 处理跨域、认证、限流等通用需求

**主要组件**：
- REST API控制器：处理实验管理、设备控制、数据查询、方法管理等接口
- WebSocket处理器：处理实时数据推送、状态更新、报警通知
- API网关：统一的请求入口，包含认证中间件、限流中间件、日志中间件

### 2. 业务逻辑层（Business Logic Layer）
**职责说明**：
- 实现核心业务逻辑和业务规则
- 协调多个服务完成业务流程
- 进行业务数据验证和处理
- 管理业务状态机和生命周期
- 触发业务事件和通知

**主要服务**：
- 实验管理服务：管理实验生命周期、方法配置验证、预处理流程
- 设备控制服务：实现泵控制逻辑、阀门控制逻辑、检测器控制逻辑
- 数据处理服务：峰检测算法、梯度计算、报告生成
- 收集管理服务：收集策略管理、试管状态管理、馏分收集控制
- 异常处理服务：报警规则判断、报警级别分类、通知发送

### 3. 服务层（Service Layer）
**职责说明**：
- 提供通用的技术服务能力
- 处理与业务无关的技术细节
- 提供消息通信、缓存、调度等基础服务
- 实现服务间的解耦和异步通信

**主要服务**：
- 消息服务：MQTT消息收发、WebSocket推送、事件总线
- 数据服务：时序数据存储、配置管理、文件存储
- 缓存服务：数据缓存、状态缓存、结果缓存
- 调度服务：定时任务调度、异步任务管理
- 安全服务：加密解密、Token管理、审计日志

### 4. 数据访问层（Data Access Layer）
**职责说明**：
- 管理数据持久化操作
- 提供统一的数据访问接口
- 处理数据库事务和连接池
- 实现数据模型映射和查询优化
- 管理数据缓存和批量操作

**主要组件**：
- 数据仓库接口：定义实验、方法、设备、用户等数据访问接口
- ORM映射：数据模型定义、关系映射、查询构建
- 数据库连接：连接池管理、事务管理、数据库迁移

### 5. 硬件抽象层（Hardware Abstraction Layer）
**职责说明**：
- 封装硬件设备的通信细节
- 提供统一的设备操作接口
- 处理设备协议转换和指令翻译
- 监控设备状态和连接健康
- 处理硬件异常和错误恢复

**主要组件**：
- 设备驱动：泵驱动、阀门驱动、检测器驱动、传感器驱动
- 通信管理：串口管理、Modbus通信、网络通信
- 设备抽象：统一的设备接口定义和实现
- 硬件监控：连接监控、健康检查、错误处理

### 请求处理流程示例 - 清洗动作

**1. 请求接收**
前端发送清洗请求到接口层，包含试管编号、清洗体积、清洗次数等参数。

**2. 接口层处理**
接口层接收到请求后，首先进行参数验证，确认请求格式正确、参数合法，然后调用业务逻辑层的设备控制服务。

**3. 业务逻辑层处理**
设备控制服务接收到清洗请求后，执行以下业务逻辑：
- 检查当前系统状态是否允许执行清洗操作
- 验证目标试管是否可以进行清洗
- 创建清洗任务记录，保存到数据库
- 调用阀门控制逻辑切换到目标试管
- 调用泵控制逻辑执行清洗流程
- 更新任务状态，发送实时状态通知

**4. 服务层支撑**
在业务处理过程中，服务层提供以下支撑：
- 消息服务发送MQTT控制指令到硬件设备
- WebSocket服务推送实时状态更新到前端
- 缓存服务缓存设备状态信息
- 调度服务管理清洗任务的执行队列

**5. 数据访问层操作**
数据访问层负责：
- 保存清洗任务记录到数据库
- 更新试管状态信息
- 记录操作日志
- 查询设备当前状态

**6. 硬件抽象层执行**
硬件抽象层执行实际的设备操作：
- 阀门驱动接收切换指令，控制阀门切换到指定试管位置
- 泵驱动接收清洗指令，控制泵按设定流速和体积执行清洗
- 传感器驱动实时采集流量、压力等数据
- 通过MQTT反馈执行状态和结果

**7. 响应返回**
清洗任务创建成功后，接口层返回任务ID和初始状态给前端。后续的执行状态通过WebSocket实时推送。

## 一、系统核心业务流程

### 1.1 主控制流程

#### 1.1.1 系统初始化流程
1. **系统启动检查**
   - 检查所有硬件设备连接状态
   - 验证通信协议（MQTT、串口、Modbus）
   - 初始化数据库连接
   - 加载系统配置参数

2. **设备自检流程**
   - 泵系统自检（压力传感器、流速传感器）
   - 检测器自检（UV灯源、波长校准）
   - 阀门系统自检（多通阀位置、电磁阀状态）
   - 收集系统自检（试管架位置、机械臂状态）

3. **通信建立流程**
   - 建立MQTT连接
   - 订阅控制主题
   - 发布设备状态主题
   - 心跳检测机制启动

### 1.2 实验生命周期管理

#### 1.2.1 实验准备阶段
**触发条件**: 用户给机器通电

**业务流程**:
1. 方法选择/创建
   - 接收前端选择的方法id
   - 或接收前端创建的新方法，创建一个新的方法id
   - 验证方法参数合理性

2. 方法参数（所有参数写到方法表中，对应好方法id）
   - 方法名称（类型：text）
   - 柱子id（类型：int）当前端返回了柱子id，要通过接口返回柱子的详细信息（型号、最大压力、死体积等）
   - 流速/ml/min（类型：int）
   - 运行时间/min（类型：int）
   - 检测器波长（类型：text,举例：[254,254]）
   - 峰驱动（类型：blob，默认False）
   - 梯度洗脱模式（类型：text，自动梯度：auto、手动梯度：manual）
   - 梯度时间表（类型：text，参数：time：单位分钟，originalA、originalB、originalC、originalD：单位百分比，flowRate：单位mL/min，例子：
     - [{"time": 0, "originalB": 80.0, "originalA": 20.0, "originalC"：0，"originalD"：0，"flowRate": 10.0}, {"time": 1, "originalB": 70.0, "originalA": 30.0, "originalC"：0，"originalD"：0， "flowRate": 10.0}]）
   - 自动梯度参数（类型：text， 参数：
           "start_ratio": 80,
            "end_ratio": 20,
            "n1_volumes": 2,
            "gradient_rate": 0.5,
            "peak_threshold": 0.1,
            "column_volume": 57, 
            "sg_window": 21,  
            "sg_order": 3,
            "baseline_window": 180,  
            "k_factor": 20）
   - 收集体积/ml（类型int）（给前端返回当前收集模块的容积）
   - 清洗体积/ml（类型int）（给前端返回当前收集模块的容积）
   - 清洗次数（类型int）

3. 系统预处理（存到预处理表中，建立一个id）
   - 吹扫系统（类型：blob，默认False）
   - 吹扫柱子（类型：blob，默认False）
   - 吹扫柱子时间（类型：int，默认5min）
   - 柱平衡（类型：blob，默认False）
   - 柱平衡时间（类型：int，默认5min）
   - 柱平衡原液（类型：text，默认A）
   - 柱平衡比例（类型：int，100%）
   - 柱平衡流速（类型：int）
  

#### 1.2.2 实验执行阶段
**触发条件**: 前端返回“开始系统预处理”
1. 找到后端存储的预处理id，找到对应的预处理方法
2. 按照：吹扫系统→吹扫柱子→柱平衡的顺序依次执行

**触发条件**: 前端返回“开始实验”

**业务流程**:
1. 实验启动
   - 记录实验开始时间
   - 创建实验数据存储结构
   - 启动数据采集线程
   - 启动设备控制线程

2. 梯度执行逻辑
   - 按梯度时间表执行
   - 实时调整泵比例（A泵/B泵）
   - 监控实际流速vs设定流速
   - 记录梯度变化点

3. 数据采集逻辑
   - UV数据采集（1Hz频率）
   - 压力数据采集（1Hz频率）
   - 流速数据实时监测
   - 数据缓存和批量存储

4. 实时控制响应
   - 用户暂停/继续指令响应
   - 梯度调整指令响应
   - 手动切换试管指令响应
   - 紧急停止指令响应

#### 1.2.3 实验结束阶段
**触发条件**: 达到设定时间或用户终止

**业务流程**:
1. 数据收尾
   - 停止数据采集
   - 保存最后缓存数据
   - 生成实验总结信息

2. 设备复位
   - 泵停止运行
   - 阀门回到初始位置
   - 检测器关闭或待机

3. 后处理
   - 管路清洗程序
   - 实验报告生成
   - 数据归档

## 二、核心业务模块逻辑

### 2.1 智能收集决策逻辑

#### 2.1.1 峰驱动收集模式
**判断逻辑**:
系统需要实时监测UV检测器数值，当检测值超过基线值加上用户设定阈值时，系统判定峰的开始。此时如果系统当前未在收集状态，则切换到新的试管开始收集，同时记录峰的起始时间点，并将系统收集状态标记为正在收集。

当UV检测值下降到基线加阈值80%以下时，系统判定峰的结束。如果系统当前处于收集状态，则结束当前试管的收集，记录峰的结束时间，计算该峰的面积数据，并将收集状态标记为未收集。

**关键参数**:
- baseline: 动态计算的基线值
- threshold: 用户设定的阈值（默认0.01AU）
- 延迟时间: 防止噪声触发的延迟（默认2秒）

#### 2.1.2 体积驱动收集模式
**判断逻辑**:
系统根据实时流速和经过的时间计算当前收集的体积。当计算得到的当前体积达到或超过用户设定的收集体积时，系统自动切换到下一个试管，重置体积计数器从零开始计算，并记录切换发生的时间点。

**关键参数**:
- set_volume: 用户设定的收集体积
- flow_rate: 实时流速
- 体积补偿: 考虑管路死体积

#### 2.1.3 人工驱动模式
**交互逻辑**:
- 监听用户切换指令
- 立即执行切换
- 记录手动操作日志

### 2.2 峰检测算法逻辑

#### 2.2.1 实时峰识别
**算法流程**:
1. 基线追踪
   - 使用滑动窗口计算基线
   - 窗口大小: 30秒
   - 更新频率: 1Hz

2. 峰起点判断
   - 斜率突变检测
   - 连续3点上升确认
   - 排除噪声干扰

3. 峰顶判断
   - 一阶导数过零点
   - 二阶导数负值确认

4. 峰终点判断
   - 返回基线水平
   - 斜率趋近于零

#### 2.2.2 峰参数计算
**计算项目**:
- 保留时间: 峰顶对应的时间
- 峰高: 峰顶值 - 基线值
- 峰面积: 积分计算
- 峰宽: 半峰高处的宽度
- 对称因子: 前沿/尾部比值

### 2.3 梯度控制逻辑

#### 2.3.1 梯度程序执行
**控制流程**:
通过梯度曲线计算每一秒的原液A\B\C\D的比例，实时调整泵的输出，确保梯度变化平滑且符合设定曲线。

#### 2.3.2 自动梯度优化
**优化策略**:
- 根据峰分离度调整梯度斜率
- 根据保留时间优化起始浓度
- 根据峰形优化流速

### 2.4 异常处理业务逻辑

#### 2.4.1 压力异常处理
**检测逻辑**:
当系统检测到压力超过最大压力限值时，将异常级别标记为严重，立即执行停泵操作，关闭进样阀门，触发报警通知，并将异常详情记录到日志系统中。

当压力超过警告压力但未达到最大压力时，系统将异常级别标记为警告，自动降低泵的流速以缓解压力，持续监控压力变化趋势，并向用户发送提醒通知。

#### 2.4.2 流速异常处理
**处理流程**:
1. 检测偏差
   - 实际流速vs设定流速
   - 偏差阈值: ±5%

2. 自动调节
   - PID控制算法
   - 逐步调整泵速
   - 最多重试3次

3. 异常上报
   - 调节失败后上报
   - 建议检查项目

#### 2.4.3 通信异常处理
**恢复机制**:
- 自动重连（最多5次）
- 数据缓存本地
- 恢复后数据同步

## 三、数据交互逻辑

### 3.1 实时数据流

#### 3.1.1 数据采集流
硬件设备产生的数据首先被数据采集服务接收，然后存入数据缓存区，当缓存达到一定数量后批量写入SQLite数据库。同时，数据采集服务将数据通过MQTT协议发布，前端通过实时订阅获取数据并更新图表显示。

#### 3.1.2 控制指令流
用户通过界面发送的控制指令首先到达RESTful API接口，然后传递给控制服务处理。控制服务通过MQTT协议将指令发布给相应的硬件设备执行。硬件执行后返回状态反馈，控制服务接收反馈后向用户界面发送指令确认信息。

### 3.2 数据存储策略

#### 3.2.1 实时数据存储
- 缓存策略: 100条数据批量写入
- 存储频率: 1秒或缓存满
- 数据压缩: 相同值合并

#### 3.2.2 历史数据管理
- 保存周期: 默认1年
- 归档策略: 3个月后压缩
- 清理策略: 用户确认后删除

### 3.3 设备通信协议

#### 3.3.1 MQTT主题设计
**发布主题**:
- `/device/pump/status` - 泵状态
- `/device/detector/data` - 检测器数据
- `/device/valve/position` - 阀门位置
- `/experiment/status` - 实验状态

**订阅主题**:
- `/control/pump/+` - 泵控制指令
- `/control/valve/+` - 阀门控制指令
- `/control/experiment/+` - 实验控制指令

#### 3.3.2 数据格式规范
**后端数据格式化服务**:
1. 消息格式定义
   - 时间戳：ISO 8601格式，精确到毫秒
   - 设备ID：唯一标识符
   - 消息类型：status/data/control/alarm
   - 数据载荷：JSON对象

2. 数据验证
   - JSON Schema验证
   - 数值范围检查
   - 必填字段验证
   - 格式错误记录

3. 示例格式
   - 泵状态消息：
     ```json
     {
         "timestamp": "2024-12-19T10:00:00.000Z",
         "device_id": "pump_01",
         "type": "status",
         "data": {
             "flow_rate": 1.5,
             "pressure": 100.5,
             "status": "running",
             "ratio_A": 20,
             "ratio_B": 80
         }
     }
     ```
   - 控制指令消息：
     ```json
     {
         "timestamp": "2024-12-19T10:00:00.000Z",
         "device_id": "valve_01",
         "type": "control",
         "command": "switch",
         "params": {
             "target_position": 5
         }
     }
     ```

## 四、用户交互逻辑

### 4.1 实验操作交互

#### 4.1.1 实验创建交互
1. 用户选择实验类型
2. 系统推荐默认参数
3. 用户调整参数
4. 系统验证参数合理性
5. 确认创建实验

#### 4.1.2 实时干预交互
**允许的实时操作**:
- 调整梯度（下一个梯度点生效）
- 手动切换试管
- 切换到废液
- 暂停/继续实验
- 紧急停止

**操作确认机制**:
- 一般操作: 直接执行
- 危险操作: 二次确认
- 不可逆操作: 输入确认码

### 4.2 数据分析交互

#### 4.2.1 实时数据查看
- 图表缩放（鼠标滚轮）
- 时间轴拖动
- 多曲线叠加显示
- 数据点详情查看

#### 4.2.2 历史数据分析
- 批量数据对比
- 峰识别和标注
- 积分参数调整
- 报告模板选择

### 4.3 系统配置交互

#### 4.3.1 方法管理
- 方法模板创建
- 参数批量修改
- 方法复制和导入
- 版本对比

#### 4.3.2 权限管理
- 角色定义（管理员/操作员/观察员）
- 功能权限分配
- 操作审计追踪

## 五、状态机管理

### 5.1 实验状态机
实验状态从待机开始，进入准备中状态，准备完成后转为运行中。运行过程中可以暂停，暂停后可恢复到运行中状态，最终实验完成。在准备中、运行中、暂停等任何状态下如果发生错误，都会转入错误终止状态。

### 5.2 设备状态机
设备状态从离线开始，连接成功后变为在线，在线后进入空闲状态。接收到任务后从空闲转为工作中，任务完成后回到空闲状态。在在线、空闲、工作中的任何状态下如果检测到设备故障，都会转入故障状态。

### 5.3 试管状态机
试管状态从空闲开始，被选中后进入准备状态，开始收集样品后转为收集中状态。当试管收集满后变为已满状态，收集结束后标记为完成。如果在收集中途需要废弃该试管的样品，状态转为废弃，废弃的试管清空后回到空闲状态。

## 六、业务规则约束

### 6.1 参数范围约束
- 流速: 0.01-10 mL/min
- 压力: 0-400 bar
- 波长: 190-800 nm
- 温度: 4-60°C
- 运行时间: 1-999 min

### 6.2 操作顺序约束
1. 必须先平衡后进样
2. 必须先设置参数后启动
3. 停止后必须清洗
4. 异常必须先处理后继续

### 6.3 安全约束
- 压力超限自动停机
- 液位不足禁止启动
- 废液满禁止继续
- 通信中断暂停实验

## 七、主要业务场景处理流程

### 7.1 实验执行流程

**场景描述**：用户选择方法并启动实验

**各层处理**：

1. **接口层**
   - 接收实验启动请求，包含方法ID、实验参数
   - 验证用户权限和参数完整性
   - 调用实验管理服务

2. **业务逻辑层**
   - 实验管理服务检查系统是否空闲
   - 加载方法配置和验证参数合理性
   - 创建实验实例，生成实验ID
   - 初始化数据存储结构
   - 启动各个控制模块（泵控制、检测器控制、收集控制）
   - 启动数据采集流程

3. **服务层**
   - 消息服务发送实验启动通知
   - 调度服务创建实验任务队列
   - 缓存服务缓存实验配置
   - WebSocket推送实验状态变化

4. **数据访问层**
   - 保存实验记录
   - 更新设备状态
   - 创建数据存储表
   - 记录操作日志

5. **硬件抽象层**
   - 初始化各个设备
   - 设置泵的初始参数
   - 启动检测器数据采集
   - 开始执行梯度程序

### 7.2 峰检测与收集流程

**场景描述**：实时检测峰并自动收集馏分

**各层处理**：

1. **硬件抽象层**
   - UV检测器持续采集数据
   - 通过MQTT上报检测数据

2. **业务逻辑层**
   - 峰检测引擎实时分析数据
   - 计算基线和判断数据变化趋势
   - 判断峰的起始和结束
   - 触发收集策略管理器
   - 发送试管切换指令

3. **服务层**
   - 缓存峰检测结果
   - 实时推送峰信息到前端
   - 记录峰参数数据

4. **数据访问层**
   - 存储峰数据
   - 更新试管状态
   - 记录馏分信息

5. **接口层**
   - 通过WebSocket推送峰检测状态
   - 提供峰数据查询接口

### 7.3 异常处理流程

**场景描述**：压力超限异常处理

**各层处理**：

1. **硬件抽象层**
   - 压力传感器检测到异常值
   - 立即上报异常信息

2. **业务逻辑层**
   - 报警服务接收异常信息
   - 判断异常级别（严重/警告/提示）
   - 执行相应处理策略
   - 严重级别：立即停止泵、关闭进样阀、暂停实验
   - 警告级别：降低流速、持续监控

3. **服务层**
   - 发送报警通知
   - 推送异常状态到前端
   - 记录异常日志

4. **数据访问层**
   - 保存异常记录
   - 更新实验状态
   - 记录处理过程

5. **接口层**
   - WebSocket推送异常通知
   - 提供异常处理确认接口

### 7.4 数据查询与分析流程

**场景描述**：查询历史实验数据并生成报告

**各层处理**：

1. **接口层**
   - 接收查询请求，包含时间范围、实验ID等条件
   - 验证查询权限

2. **业务逻辑层**
   - 数据处理服务构建查询条件
   - 调用数据分析引擎
   - 计算峰参数、积分面积
   - 生成分析报告

3. **服务层**
   - 从缓存获取常用数据
   - 处理大数据量分页
   - 生成报告文件

4. **数据访问层**
   - 执行时序数据查询
   - 获取峰数据和馏分信息
   - 读取实验配置

5. **接口层**
   - 返回查询结果
   - 提供报告下载接口

## 八、性能优化策略

### 8.1 数据处理优化
- 批量数据处理减少IO
- 异步处理不阻塞主线程
- 数据压缩减少存储
- 索引优化提升查询

### 8.2 实时响应优化
- 控制指令优先队列
- 关键数据实时推送
- 非关键数据延迟处理
- 界面局部更新

### 8.3 资源管理优化
- 连接池管理
- 内存缓存策略
- 定时清理机制
- 负载均衡策略