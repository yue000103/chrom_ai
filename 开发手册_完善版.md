# 液相色谱仪控制系统开发手册 V2.0

## 1. 产品概述

### 1.1 产品定位
液相色谱仪控制系统是一款面向分析实验室的专业自动化控制软件，专注于提供高效、精准、智能的色谱分析解决方案。系统采用B/S架构，支持远程操作和实时监控，实现从样品进样到数据分析的全流程自动化。

### 1.2 核心价值
- **自动化程度高**：减少80%以上的人工操作
- **分析精度高**：智能峰检测算法，准确率>99%
- **操作效率高**：支持批量样品连续分析
- **数据可追溯**：完整的审计追踪和数据管理

### 1.3 系统架构
```
┌─────────────────────────────────────────────┐
│           Web前端（Vue.js + ECharts）          │
├─────────────────────────────────────────────┤
│         WebSocket实时通信层                    │
├─────────────────────────────────────────────┤
│     Flask后端服务 + SocketIO                  │
├─────────────────────────────────────────────┤
│         业务逻辑层                             │
│  ┌──────────┬──────────┬──────────────┐     │
│  │设备管理   │实验控制   │数据处理      │     │
│  └──────────┴──────────┴──────────────┘     │
├─────────────────────────────────────────────┤
│         设备驱动层                             │
│  ┌──────────┬──────────┬──────────────┐     │
│  │Modbus RTU│TCP/IP    │串口通信       │     │
│  └──────────┴──────────┴──────────────┘     │
├─────────────────────────────────────────────┤
│         数据存储层（SQLite + 文件系统）         │
└─────────────────────────────────────────────┘
```

## 2. 详细业务逻辑

### 2.1 系统初始化流程

#### 2.1.1 启动检查逻辑
```python
系统启动
├─ 1. 配置文件检查
│   ├─ 验证config.yaml完整性
│   ├─ 加载设备配置文件（valves.json, pumps.json等）
│   └─ 验证配置参数合法性
│
├─ 2. 设备连接初始化
│   ├─ 扫描可用串口
│   ├─ 尝试连接各设备（最多重试3次）
│   ├─ 读取设备固件版本
│   └─ 同步设备时钟
│
├─ 3. 状态恢复检查
│   ├─ 检查是否有未完成的实验
│   │   ├─ 是 → 提示用户选择
│   │   │   ├─ 继续实验（恢复现场）
│   │   │   └─ 放弃实验（清理状态）
│   │   └─ 否 → 进入待机状态
│   │
│   └─ 加载最后使用的方法
│
└─ 4. 系统自检
    ├─ UV灯源使用时长检查
    ├─ 压力传感器校准检查
    ├─ 阀门位置复位
    └─ 生成启动日志
```

#### 2.1.2 设备初始化优先级
1. **UV检测器**（最高优先级）- 需要预热时间
2. **压力传感器** - 安全监控
3. **高压泵** - 核心驱动设备
4. **多通阀** - 流路控制
5. **其他传感器** - 辅助监控

### 2.2 方法管理业务逻辑

#### 2.2.1 方法创建流程
```python
创建新方法
├─ 1. 基础参数设置
│   ├─ 方法名称（唯一性检查）
│   ├─ 方法描述
│   ├─ 创建时间/创建人
│   └─ 方法类型（等度/梯度）
│
├─ 2. 色谱条件设置
│   ├─ 柱子选择
│   │   ├─ 从柱子库选择
│   │   ├─ 自动加载柱子参数
│   │   └─ 压力限制自动设置
│   │
│   ├─ 流动相设置
│   │   ├─ 溶剂选择（A/B/C/D）
│   │   ├─ 初始比例
│   │   └─ 流速设置（验证柱子承受范围）
│   │
│   └─ 检测条件
│       ├─ 波长设置（支持多波长）
│       ├─ 采样频率（1-10Hz）
│       └─ 运行时间
│
├─ 3. 梯度程序设置（如果选择梯度）
│   ├─ 时间节点设置
│   ├─ 比例变化曲线
│   ├─ 流速变化（可选）
│   └─ 曲线类型（线性/曲线）
│
├─ 4. 试管收集设置
│   ├─ 切换模式
│   │   ├─ 时间切换（固定间隔）
│   │   ├─ 体积切换（固定体积）
│   │   ├─ 峰切换（智能识别）
│   │   └─ 手动切换
│   │
│   └─ 收集参数
│       ├─ 收集体积阈值
│       ├─ 峰检测灵敏度
│       └─ 废液处理策略
│
└─ 5. 方法验证与保存
    ├─ 参数合理性检查
    ├─ 硬件兼容性验证
    ├─ 保存到方法库
    └─ 生成方法指纹（MD5）
```

#### 2.2.2 方法执行预检查
```python
方法执行前检查
├─ 硬件状态检查
│   ├─ 所有设备在线
│   ├─ 压力在正常范围
│   └─ 流动相充足
│
├─ 方法参数验证
│   ├─ 流速不超过柱子限制
│   ├─ 压力不超过系统限制
│   └─ 运行时间合理
│
└─ 资源检查
    ├─ 试管数量充足
    ├─ 存储空间充足
    └─ 废液瓶未满
```

### 2.3 实验执行业务逻辑

#### 2.3.1 润柱过程控制
```python
润柱流程
├─ 1. 润柱参数设置
│   ├─ 获取柱子规格
│   ├─ 计算推荐流速（柱子最大流速的50-80%）
│   ├─ 设置平衡时间（默认30分钟）
│   └─ 选择润柱溶剂
│
├─ 2. 润柱执行
│   ├─ 启动泵（缓慢提升流速）
│   ├─ 监控压力变化
│   │   ├─ 压力稳定性判断（±5%波动）
│   │   └─ 异常压力处理
│   │
│   ├─ 基线监控
│   │   ├─ UV基线漂移检测
│   │   ├─ 基线稳定判定（<0.001AU/min）
│   │   └─ 自动调零
│   │
│   └─ 时间控制
│       ├─ 最小润柱时间保证
│       └─ 最大润柱时间限制
│
└─ 3. 润柱完成判定
    ├─ 压力稳定（连续5分钟波动<5%）
    ├─ 基线稳定（漂移<0.001AU/min）
    ├─ 达到设定时间
    └─ 生成润柱报告
```

#### 2.3.2 洗脱曲线采集核心逻辑
```python
数据采集主循环（10Hz）
├─ 1. 数据获取
│   ├─ UV吸光度值
│   ├─ 系统压力值
│   ├─ 实际流速
│   ├─ 温度（如有）
│   └─ 时间戳
│
├─ 2. 数据处理
│   ├─ 滤波处理（移动平均）
│   ├─ 基线扣除
│   ├─ 噪音评估
│   └─ 数据缓存
│
├─ 3. 实时分析
│   ├─ 峰检测算法
│   │   ├─ 斜率分析
│   │   ├─ 峰起始判定
│   │   ├─ 峰顶识别
│   │   └─ 峰结束判定
│   │
│   ├─ 积分计算
│   │   ├─ 峰面积
│   │   ├─ 峰高
│   │   └─ 保留时间
│   │
│   └─ 触发动作
│       ├─ 试管切换触发
│       ├─ 报警触发
│       └─ 数据标记
│
├─ 4. 梯度控制（如果是梯度洗脱）
│   ├─ 时间点检查
│   ├─ 比例计算（线性插值）
│   ├─ 泵指令发送
│   └─ 延迟补偿
│
└─ 5. 数据推送
    ├─ WebSocket实时推送
    ├─ 图表更新数据
    └─ 存储队列写入
```

#### 2.3.3 智能峰检测算法逻辑
```python
峰检测状态机
├─ 状态1：基线状态
│   ├─ 持续监测信号变化
│   ├─ 计算基线噪音水平
│   └─ 当斜率>阈值 → 进入状态2
│
├─ 状态2：峰上升期
│   ├─ 记录峰起始点
│   ├─ 跟踪斜率变化
│   ├─ 当斜率<0 → 进入状态3
│   └─ 触发试管切换准备
│
├─ 状态3：峰顶期
│   ├─ 识别峰顶位置
│   ├─ 记录峰顶时间和高度
│   └─ 当信号持续下降 → 进入状态4
│
├─ 状态4：峰下降期
│   ├─ 跟踪下降趋势
│   ├─ 判断是否有肩峰
│   └─ 当信号接近基线 → 进入状态5
│
└─ 状态5：峰结束
    ├─ 确定峰结束点
    ├─ 计算峰参数
    ├─ 触发试管切换
    └─ 返回状态1
```

### 2.4 试管管理业务逻辑

#### 2.4.1 试管切换决策树
```python
试管切换决策
├─ 触发条件判断
│   ├─ 时间触发
│   │   └─ 当前时间 - 上次切换时间 >= 设定间隔
│   │
│   ├─ 体积触发
│   │   └─ 累计体积 >= 设定体积
│   │
│   ├─ 峰检测触发
│   │   ├─ 峰开始 → 新试管
│   │   └─ 峰结束 → 切换
│   │
│   └─ 手动触发
│       └─ 用户命令
│
├─ 切换前检查
│   ├─ 下一个试管可用性
│   ├─ 阀门状态正常
│   └─ 无其他操作冲突
│
├─ 执行切换
│   ├─ 记录当前试管信息
│   ├─ 发送阀门切换指令
│   ├─ 等待切换完成（典型500ms）
│   └─ 更新试管状态
│
└─ 切换后处理
    ├─ 标记试管内容
    ├─ 记录切换时间
    ├─ 更新UI显示
    └─ 准备下一个试管
```

#### 2.4.2 试管内容管理
```python
试管信息记录
├─ 基础信息
│   ├─ 试管编号
│   ├─ 开始时间
│   ├─ 结束时间
│   └─ 收集体积
│
├─ 样品信息
│   ├─ 对应的峰编号
│   ├─ 保留时间范围
│   ├─ 峰面积
│   └─ 纯度估算
│
└─ 操作记录
    ├─ 收集/保留/废弃状态
    ├─ 操作人员
    └─ 备注信息
```

### 2.5 异常处理业务逻辑

#### 2.5.1 异常分级处理
```python
异常处理策略
├─ 级别1：提示级
│   ├─ 基线轻微漂移
│   ├─ 压力小幅波动
│   └─ 处理：记录日志，显示提示
│
├─ 级别2：警告级
│   ├─ 压力接近上限（80%）
│   ├─ 流速偏差>10%
│   ├─ 试管即将用完
│   └─ 处理：黄色警告，可继续运行
│
├─ 级别3：错误级
│   ├─ 设备通信中断
│   ├─ 压力超限（>90%）
│   ├─ 流速异常（偏差>20%）
│   └─ 处理：暂停实验，等待处理
│
└─ 级别4：严重级
    ├─ 压力超过安全限制
    ├─ 检测到漏液
    ├─ 多个设备故障
    └─ 处理：紧急停机，保存数据
```

#### 2.5.2 异常恢复机制
```python
异常恢复流程
├─ 1. 状态评估
│   ├─ 记录异常时系统状态
│   ├─ 评估数据完整性
│   └─ 判断是否可恢复
│
├─ 2. 恢复尝试
│   ├─ 通信异常 → 自动重连（3次）
│   ├─ 压力异常 → 降低流速
│   ├─ 基线异常 → 自动调零
│   └─ 阀门异常 → 复位重试
│
├─ 3. 恢复成功
│   ├─ 恢复到异常前状态
│   ├─ 继续实验
│   └─ 记录恢复日志
│
└─ 4. 恢复失败
    ├─ 保存当前数据
    ├─ 生成异常报告
    ├─ 通知用户
    └─ 进入安全模式
```

### 2.6 数据管理业务逻辑

#### 2.6.1 数据存储策略
```python
数据分层存储
├─ 实时数据层（内存）
│   ├─ 最近1000个数据点
│   ├─ 实时峰检测缓存
│   └─ 当前梯度状态
│
├─ 缓存层（Redis/内存数据库）
│   ├─ 当前实验完整数据
│   ├─ 实时计算结果
│   └─ 设备状态快照
│
├─ 持久层（SQLite）
│   ├─ 实验元数据
│   ├─ 方法参数
│   ├─ 峰表数据
│   └─ 用户操作日志
│
└─ 文件层（文件系统）
    ├─ 原始数据文件（.csv）
    ├─ 色谱图文件（.png）
    ├─ PDF报告
    └─ 数据备份
```

#### 2.6.2 数据完整性保证
```python
数据保护机制
├─ 采集阶段
│   ├─ 双缓冲机制
│   ├─ 数据校验和
│   └─ 时间戳同步
│
├─ 传输阶段
│   ├─ WebSocket心跳检测
│   ├─ 数据包序号
│   └─ 断线重传
│
├─ 存储阶段
│   ├─ 事务处理
│   ├─ 定期自动保存
│   └─ 增量备份
│
└─ 审计追踪
    ├─ 操作日志（谁、什么时候、做了什么）
    ├─ 数据修改记录
    └─ 电子签名（可选）
```

### 2.7 用户权限业务逻辑

#### 2.7.1 权限模型
```python
用户权限体系
├─ 角色定义
│   ├─ 管理员
│   │   └─ 全部权限
│   ├─ 操作员
│   │   ├─ 执行实验
│   │   ├─ 查看数据
│   │   └─ 创建方法
│   └─ 访客
│       └─ 只读权限
│
├─ 权限控制点
│   ├─ 设备控制
│   ├─ 方法管理
│   ├─ 实验执行
│   ├─ 数据导出
│   └─ 系统配置
│
└─ 访问控制
    ├─ 单一控制原则（一台仪器同时只能一人操作）
    ├─ 会话管理
    └─ 操作互斥锁
```

## 3. 特殊业务场景

### 3.1 吹扫系统流程（完善版）
```python
吹扫系统执行
├─ 1. 启动条件检查
│   ├─ 系统空闲（无实验运行）
│   ├─ 用户确认
│   └─ 气源压力正常
│
├─ 2. 吹扫模式选择
│   ├─ 试管吹扫
│   │   ├─ 逐个试管检测
│   │   ├─ 气泡传感器监控
│   │   ├─ 无液体信号>5秒
│   │   └─ 切换下一试管
│   │
│   └─ 柱子吹扫
│       ├─ 关闭流动相
│       ├─ 开启气源
│       ├─ 监控出口
│       └─ 定时结束
│
├─ 3. 吹扫执行监控
│   ├─ 压力监控（防止超压）
│   ├─ 流量监控
│   └─ 时间控制
│
└─ 4. 吹扫完成
    ├─ 关闭气源
    ├─ 系统复位
    └─ 生成吹扫记录
```

### 3.2 批量样品处理
```python
批量分析流程
├─ 1. 批次设置
│   ├─ 样品列表导入
│   ├─ 方法分配
│   └─ 进样顺序设置
│
├─ 2. 序列执行
│   ├─ 自动进样器控制
│   ├─ 方法自动切换
│   ├─ 柱间平衡
│   └─ 数据自动命名
│
└─ 3. 批次管理
    ├─ 进度监控
    ├─ 异常样品标记
    └─ 批次报告生成
```

### 3.3 条件预测集成
```python
AI预测集成
├─ 1. SMILES输入
│   └─ 化合物结构输入
│
├─ 2. 条件预测
│   ├─ 调用预测API
│   ├─ 获取推荐条件
│   └─ 参数映射
│
└─ 3. 方法生成
    ├─ 自动创建方法
    ├─ 参数优化建议
    └─ 保存到方法库
```

## 4. 性能指标要求

### 4.1 响应时间要求
- 数据采集延迟：<10ms
- UI响应时间：<100ms
- 设备指令响应：<50ms
- 图表刷新率：10fps

### 4.2 数据处理能力
- 采样率：1-10Hz可调
- 并发数据流：最少4路（UV、压力、流速、温度）
- 数据缓存：至少30分钟数据
- 文件大小：单次实验<100MB

### 4.3 稳定性要求
- 连续运行：>72小时
- 数据丢失率：<0.01%
- 自动恢复成功率：>95%
- 设备重连时间：<30秒

## 5. 报告生成规范

### 5.1 标准分析报告内容
```
报告结构
├─ 报告头
│   ├─ 实验编号
│   ├─ 分析日期
│   ├─ 操作人员
│   └─ 仪器信息
│
├─ 方法信息
│   ├─ 方法名称
│   ├─ 色谱条件
│   ├─ 梯度表（如有）
│   └─ 检测参数
│
├─ 色谱图
│   ├─ 完整色谱图
│   ├─ 放大图（主要峰）
│   └─ 梯度曲线图
│
├─ 峰表
│   ├─ 峰编号
│   ├─ 保留时间
│   ├─ 峰面积
│   ├─ 峰高
│   ├─ 相对面积%
│   └─ 分离度
│
├─ 试管信息
│   ├─ 试管编号
│   ├─ 对应峰
│   └─ 收集体积
│
└─ 质量参数
    ├─ 基线噪音
    ├─ 系统压力曲线
    └─ 方法重现性
```

## 需要的补充资料

<function_calls>
<invoke name="TodoWrite">
<parameter name="todos">[{"content": "Analyze current development manual structure and identify gaps", "status": "completed", "activeForm": "Analyzing current development manual structure"}, {"content": "Create enhanced development manual with detailed business logic", "status": "completed", "activeForm": "Creating enhanced development manual"}, {"content": "List required additional documentation and resources", "status": "in_progress", "activeForm": "Listing required documentation"}]