请根据我的建议修改数据库：
1. device_config 无需更改
2. sensor_data 去掉device_id:，无需关联设备，data_type:只有压力、液体状态，value:类型是压力的时候是压力值，是液体状态的是0/1，0代表无液体，1代表有液体，raw_data:不要这个
3. experiments  experiment_type:这个包括分析和制备，status:这个包含已结束，未结束（以后用来恢复状态），去掉- parameters: 实验参数(JSON格式)，results: 实验结果(JSON格式)，加上洗脱曲线（类型：text，举例：[12，13，14，14，80],每个值代表一秒的比例），试管操作（类型：text,举例：[1,2,3,4],每一个值代表一个试管操作id，关联试管操作表）
4. analysis_methods  去掉
5. chromatography_peaks 这个加上检测器信号值（类型：text，举例：[0.12,0.35,0.69,1.55,]每个值代表一秒的数据），峰数量：int类型
6. system_logs 无需更改
7.calibration_data 去掉
8. tube_info 这个改成试管架信息，里面有参数：试管架id、试管容积、试管数量，其他的全去掉
9. mqtt_messages 我担心每一秒产生一个数据，写入一次会对性能有影响，你看怎么修改合适
10. data_quality_metrics 无需更改

加一个柱子信息表：柱子id（类型：int，主键，自增），柱子编码（类型：text），规格/G（类型：int），最大压力（类型：int，单位：bar），流速ml/min（类型：int），柱体积CV（ml）：（类型：int），上样量（类型：text）
加一个试管操作信息表：试管操作id主键，自增，试管id（类型：text），试管操作类型（类型：text）

方法表（所有参数写到方法表中，对应好方法id）
   - 方法名称（类型：text）
   - 柱子id（类型：int）当前端返回了柱子id，要通过接口返回柱子的详细信息（型号、最大压力、死体积等）
   - 流速/ml/min（类型：int）
   - 运行时间/min（类型：int）
   - 检测器波长（类型：text,举例：[254,254]）
   - 峰驱动（类型：blob，默认False）
   - 梯度洗脱模式（类型：text，自动梯度：auto、手动梯度：manual）
   - 梯度时间表（类型：text，参数：time：单位分钟，originalA、originalB、originalC、originalD：单位百分比，flowRate：单位mL/min，例子：
     - [{"time": 0, "originalB": 80.0, "originalA": 20.0, "originalC"：0，"originalD"：0，"flowRate": 10.0}, {"time": 1, "originalB": 70.0, "originalA": 30.0, "originalC"：0，"originalD"：0， "flowRate": 10.0}]）
   - 自动梯度参数（类型：text， 参数：
           "start_ratio": 80,
            "end_ratio": 20,
            "n1_volumes": 2,
            "gradient_rate": 0.5,
            "peak_threshold": 0.1,
            "column_volume": 57, 
            "sg_window": 21,  
            "sg_order": 3,
            "baseline_window": 180,  
            "k_factor": 20）
   - 收集体积/ml（类型int）（给前端返回当前收集模块的容积）
   - 清洗体积/ml（类型int）（给前端返回当前收集模块的容积）
   - 清洗次数（类型int）