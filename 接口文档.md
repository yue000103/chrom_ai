柱子的操作接口文档
 - 列表：GET /api/columns/
  - 单个：GET /api/columns/{id}
  - 搜索：GET /api/columns/search
  - 统计：GET /api/columns/statistics
  - 创建：POST /api/columns/
  - 更新：PUT /api/columns/{id}
  - 删除：DELETE /api/columns/{id}

核心控制接口：
  - POST /api/experiment/runtime/start - 启动实验运行时
  - POST /api/experiment/runtime/stop - 停止实验（情况2）
  - POST /api/experiment/runtime/pause - 暂停实验
  - POST /api/experiment/runtime/resume - 恢复实验

  状态查询接口：
  - GET /api/experiment/runtime/status/{experiment_id} - 获取实验状态
  - GET /api/experiment/runtime/current - 获取当前运行的实验
  - GET /api/experiment/runtime/signal/{experiment_id} - 获取信号数据
  - GET /api/experiment/runtime/health - 系统健康检查


rack_info API端点 :
  - POST /api/racks/ - 创建试管架
  - GET /api/racks/ - 获取列表
  - GET /api/racks/{rack_id} - 获取详情
  - PUT /api/racks/{rack_id} - 更新信息
  - DELETE /api/racks/{rack_id} - 删除试管架
  - GET /api/racks/health - 健康检查



实验数据管理接口路径：
  - GET /api/experiment-data/ - 获取所有实验信息
  - GET /api/experiment-data/{experiment_id} - 获取单个实验详情
  - POST /api/experiment-data/ - 新增实验数据
  - PUT /api/experiment-data/{experiment_id} - 修改实验数据（除method_id外）
  - DELETE /api/experiment-data/{experiment_id} - 删除实验数据
  - GET /api/experiment-data/health - 健康检查

实验控制接口 (/api/experiments)

  - PUT /api/experiments/{experiment_id}/status - 更新实验状态
  - POST /api/experiments/runtime/start - 启动实验运行时
  - POST /api/experiments/runtime/stop - 停止实验运行时
  - POST /api/experiments/runtime/resume - 继续实验运行时
  - GET /api/experiments/runtime/status/{experiment_id} - 获取实验运行时状态
  - GET /api/experiments/runtime/current - 获取当前运行的实验
  - GET /api/experiments/health - 健康检查

 接口路径会是：
  - POST /api/method-control/
  - GET /api/method-control/
  - GET /api/method-control/{method_id}
  - PUT /api/method-control/{method_id}
  - DELETE /api/method-control/{method_id}


 API接口列表：

  1. POST /method-control/ - 创建新方法
    - method_id自动递增，不需要手动指定
    - 支持所有必要的方法参数
  2. GET /method-control/ - 获取方法列表
    - 支持按方法名和梯度模式筛选
    - 支持分页限制
  3. GET /method-control/{method_id} - 获取单个方法详情
    - 根据method_id查询
  4. PUT /method-control/{method_id} - 更新方法
    - method_id不可修改（路径参数）
    - 只更新提供的字段，支持部分更新
  5. DELETE /method-control/{method_id} - 删除方法
    - 根据method_id删除




1. 全局实验管理器

  - 添加了全局的ExperimentFunctionManager实例
  - 实现单例模式，确保系统中只有一个实验管理器

  2. 启动实验接口 (/start/{experiment_id})

  - 检查系统是否已有实验在运行
  - 创建ExperimentConfig对象
  - 调用exp_manager.start_experiment()启动实验
  - 返回实时的进度和步骤信息

  3. 暂停实验接口 (/pause/{experiment_id})

  - 验证当前运行的实验ID
  - 调用exp_manager.pause_experiment()
  - 实际暂停积分和梯度执行

  4. 恢复实验接口 (/resume/{experiment_id})

  - 检查实验是否处于可恢复状态
  - 调用exp_manager.resume_experiment()
  - 恢复积分和梯度执行（部分功能待完善）

  5. 终止实验接口 (/terminate/{experiment_id})

  - 检查实验状态
  - 调用exp_manager.stop_experiment()
  - 完全停止所有实验相关进程

  6. 状态查询接口 (/status/{experiment_id})

  - 优先从运行时状态获取详细信息
  - 返回实时的进度、当前步骤、阶段信息
  - 包含暂停状态、进度百分比等详细信息

  7. 接口特性

  - 实时状态: 从ExperimentFunctionManager获取真实的运行状态
  - 错误处理: 完善的异常处理和状态验证
  - 数据库同步: API调用成功后同步更新数据库状态
  - 日志记录: 完整的操作日志记录

  现在前端可以通过这些API接口实现：
  - 启动实验（包含预处理 → 正式实验流程）
  - 实时暂停/恢复（暂停积分和梯度执行）
  - 终止实验（停止所有相关进程）
  - 获取详细的实验运行状态和进度
